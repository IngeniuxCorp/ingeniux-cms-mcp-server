# Ingeniux CMS MCP Server Local Development Configuration

# Local mock/test endpoints
CMS_BASE_URL=http://localhost:4000/mock-cms
OAUTH_CLIENT_ID=dev-client-id
OAUTH_CLIENT_SECRET=dev-client-secret
OAUTH_REDIRECT_URI=http://localhost:3000/callback

# Local development settings
API_TIMEOUT=10000
MAX_RETRIES=1
LOG_LEVEL=debug
CACHE_TTL=60
RATE_LIMIT_RPM=1000
PORT=3000
HOST=localhost

# Security (for local only, use a random string)
TOKEN_ENCRYPTION_KEY=dev_local_32_char_encryption_key